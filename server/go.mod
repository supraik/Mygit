// go.mod defines a Go module - this is Go's package management system
// Like package.json (Node) or Cargo.toml (Rust)

module mygit-server

// Go version required (1.21 or later recommended for latest features)
go 1.21
